1.为什么自定义函数前和括号内都要加个void

函数名前	void myFunction(...)	表示这个函数没有返回值（不返回任何数据）
括号内	myFunction(void)	表示这个函数不接受任何参数（调用时不需要传参）



2.返回值到底啥意思,为什么main函数有返回值,而自定义函数没有返回值

返回值就是函数执行完之后，给调用者（调用它的地方）的一个结果
main 函数的返回值是给操作系统看的，用来告诉系统程序运行是否正常
main 需要告诉操作系统执行结果，所以有返回值；普通函数按需决定，不需要就不返回。



3.如果一段代码中有多个main函数,先调用哪个?
答案是：编译会直接报错，根本运行不了。

C 语言规定：一个程序只能有一个 main 函数，它是程序的唯一入口。如果你写了两个 main，编译器会报“重复定义”的错误（multiple definition of 'main'）。

所以不存在“先调用哪个”的问题——因为连编译都过不了。



4.用户本身输入浮点数却可以转换成指数或者16进制,为什么上面那个浮点数转整数就不行了?

因为“浮点数 → 指数/十六进制”是在“同一类型内部”换一种表示方式，而“浮点数 → 整数”是“跨类型”的强制转换，两者性质完全不同


